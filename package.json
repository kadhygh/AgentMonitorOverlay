{
  "name": "agent-monitor-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Local Agent Monitor Overlay workspace.",
  "scripts": {
    "broker": "node broker/server.js",
    "broker:verify": "powershell -ExecutionPolicy Bypass -File scripts/broker/verify.ps1"
  },
  "engines": {
    "node": ">=18"
  }
}
